根据你的《LeiaPix AI 复刻版》完整项目开发文档，我将整个项目的落地开发工作拆分为以下 6 个可执行的、高度细化的技术模块（Task-by-Task 研发排期）。你可以直接将以下拆分细则作为项目管理工具（如 Jira 或 PingCode）的任务（Issues）基线进行分发。步骤一：基础设施部署与项目脚手架初始化（第 1 周）任务 1.1：目录结构创建与版本控制管理开发细则：严格按照文档规定的目录结构，在本地或 Git 仓库中初始化根目录。分别创建 frontend/、backend/、ai-models/、deploy/ 等核心文件夹。配置标准：初始化 .gitignore（过滤 node_modules、.venv、模型权重文件 .pth/.onnx 以及本地 .env 环境配置文件）。编写代码规范配置文件（前端配置 ESLint + Prettier，后端配置 Ruff + mypy）。任务 1.2：基于 Docker Compose 的中间件集群拉起开发细则：在 /deploy/docker/ 目录下编写 docker-compose.yml 配置文件。启动服务清单：PostgreSQL 16：作为主关系型数据库，配置端口 5432。Redis 7-alpine：作为任务队列 broker 及缓存，配置端口 6379。MinIO：作为对象存储，配置 API 端口 9000，控制台端口 9001。MongoDB 7：作为分享数据文档存储，配置端口 27017。初始化脚本：启动后登录 MinIO 控制台，创建名为 leiapix-storage 的存储桶（Bucket），并在内部建立 images/original/、images/thumbnail/、depth_maps/、scenes/、textures/ 及 exports/ 子目录结构。任务 1.3：后端 FastAPI 框架与基础环境初始化开发细则：使用 poetry 或 pip 在 backend/ 目录下初始化 Python 3.11+ 开发环境。安装基础依赖包（fastapi、uvicorn、sqlalchemy、alembic、celery、pydantic-settings）。配置文件：编写 app/core/config.py，利用 pydantic-settings 读取环境变量（包括数据库连接串、MinIO 密钥、JWT 密钥等）。任务 1.4：前端 Vite + React 架构及代理配置开发细则：在 frontend/ 目录下使用 npm create vite@latest -- --template react-ts 构建脚手架。安装 tailwindcss、shadcn/ui 基础 UI 组件库，以及状态管理库 zustand。跨域代理：在 vite.config.ts 中配置 server.proxy，将所有指向 /api/v1 的请求反向代理至本地后端 http://localhost:8000，彻底解决开发环境跨域问题。步骤二：数据模型设计与数据库迁移（第 2 周）任务 2.1：PostgreSQL 关系型表结构编写开发细则：在 backend/app/models/ 目录下，使用 SQLAlchemy 2.0 编写实体类映射。实现表清单：users 表：主键 UUID、用户名、邮箱、密码哈希（使用 bcrypt）、套餐类型（free/pro）。images 表：记录原图元数据、存储 Key、宽高以及处理状态。depth_maps 表：外键关联 images.id，记录所用 AI 模型名称及深度图下载 URL。scenes 表：存储渲染及动画配置的 JSONB 字段。tasks 表：用于异步监控队列进度的任务状态表。exports 表：记录媒体文件（MP4/GIF）的生成和有效期。索引优化：为 users.email、images.user_id、tasks.status 等频繁查询的列显式创建数据库索引。任务 2.2：Alembic 数据库迁移版本控制开发细则：在 backend/ 目录下执行 alembic init alembic。配置 alembic.ini 中的数据库连接字符串。执行迁移：在 alembic/env.py 中引入 app.models 的 Base 元数据，执行 alembic revision --autogenerate -m "init_db" 生成首个迁移脚本，随后执行 alembic upgrade head 在容器数据库中自动生成所有物理表。任務 2.3：MongoDB 与 Redis 缓存配置开发细则：在后端配置 MongoDB 客户端连接（使用 motor 或 pymongo）。参考文档设计，创建 shares 集合，用于后续支持存储非结构化的免登录 3D 分享数据。缓存设计：编写 Redis 工具类，实现对任务进度 task:{task_id}（Hash 类型）以及 API 请求频率计数器 rate_limit:{user_id}:{endpoint} 的读写封装。步骤三：AI 推理引擎与 Celery 异步任务闭环（第 3 周）任务 3.1：AI 模型权重准备与隔离加载开发细则：在 ai-models/ 对应路径下下载并放置 Depth Anything V2 (v2.0, ~350MB) 的 PyTorch 权重文件。推理封装：在 ai-models/inference_engine.py 中编写通用的模型加载类 InferenceEngine。利用 PyTorch 的 torch.device("cuda" if torch.cuda.is_available() else "cpu") 自动适配硬件加速。实现图像预处理（尺寸缩放、归一化）和前向传播（Forward Pass）逻辑。任务 3.2：深度图优化后处理算法编写开发细则：模型输出原始深度矩阵后，利用 opencv-python 的双边滤波函数（cv2.bilateralFilter）对深度图进行边缘保持平滑处理，防止网格化噪点。归一化映射：计算深度矩阵的极值，将数值等比例线性映射到 [0, 255] 灰度区间，使用 Pillow 库将其转换为单通道 8 位 PNG 图像，准备上传对象存储。任务 3.3：Celery 队列与 GPU 资源并发控制开发细则：在 backend/app/core/ 配置文件中声明 Celery 路由策略，严格划分独立队列：将 estimate_depth 任务绑定至 gpu_depth 队列，限制并发数 CELERY_WORKER_CONCURRENCY = 2，以防止单张 GPU 显存溢出（OOM）。任务编码：编写 backend/app/tasks/depth_task.py。该任务的生命周期为：从小程序/前端传入 image_id → 从 MinIO 下载原图 → 调用推理引擎生成深度图 → 将结果 PNG 上传至 MinIO depth_maps/ → 更新 PostgreSQL 状态。任务 3.4：FastAPI WebSocket 实时状态流开发细则：在后端利用 FastAPI 的 APIRouter 实现 WebSocket 路由 /api/v1/ws。状态推送：Celery 异步任务在执行时，按步长计算进度值（如：预处理 20% -> 模型推理 60% -> 后处理 90%），并将进度字典写入 Redis 缓存。WebSocket 服务轮询或监听 Redis 消息发布，向客户端推送类型为 task_progress 的实时数据包。步骤四：后端核心 RESTful API 接口实现（第 4 周）任务 4.1：图片上传服务（/api/v1/upload）开发细则：实现接收 multipart/form-data 的 POST 接口。业务逻辑校验：校验文件大小（必须 $\le$ 20MB）。利用文件的 Magic Bytes 校验其实际格式是否为 JPG/PNG/WEBP。利用 Pillow 自动读取 EXIF 信息修复旋转纠偏，并将图片等比缩放至合理的基准分辨率（若超出 8192px）。异步生成一张长边为 256px 的缩略图，原图与缩略图一并写入 MinIO。写入数据库 images 表并返回 image_id。任务 4.2：触发深度估计服务（/api/v1/depth/estimate）开发细则：实现接收 JSON 载荷的 POST 接口。业务逻辑：读取 image_id 和指定模型参数，向 PostgreSQL 任务表插入一条 status='pending' 的记录，随后通过 .delay() 异步唤起 Celery 的深度估计任务，立即向前端响应 200 OK 并附带 task_id。任务 4.3：3D 场景数据初始化服务（/api/v1/render/3d）开发细则：接收 image_id 和 depth_map_id。数据组装：校验两张图片均已生成完毕后，在后端组装基础 3D 渲染元数据，包括默认的视场角（fov=60）、视差缩放倍率等参数，将场景元数据序列化为 JSON 字符串写入 MinIO 的 scenes/ 目录，最后在关系型数据库 scenes 表中插入记录。任务 4.4：认证授权与请求限流挂载开发细则：使用 python-jose 实现 JWT 访问令牌与刷新令牌的双 Token 认证签发体系。中间件限流：编写 FastAPI 依赖注入项，对 /upload、/depth/estimate 等 P0/P1 核心计算接口挂载基于 Redis 计数器的限流器（例如对免费用户限制 10次/小时 的上传）。步骤五：前端 Three.js 渲染引擎与视差映射（第 5 周）任务 5.1：Zustand 全局状态机与任务轮询组件开发细则：在前端 frontend/src/stores/editorStore.ts 中建立编辑器的核心状态。核心逻辑：封装 uploadImage 与 estimateDepth 的 Axios 请求函数。当接口返回 task_id 后，建立 WebSocket 连接；若 WebSocket 断开，则自动降级为每 2 秒请求一次后端任务状态接口的轮询机制，直到 processingStep 变为 ready。任务 5.2：React Three Fiber 场景画布初始化开发细则：在 components/preview/ 目录下创建 Scene3D.tsx 组件。画布构建：引入 @react-three/fiber 的 <Canvas> 组件。在内部挂载 <perspectiveCamera>（设置基础视角参数）以及来自 @react-three/drei 的 <OrbitControls>（配置鼠标拖拽、滚轮缩放等交互约束，防止相机移出视锥体）。任务 5.3：基于自定义顶点着色器（Vertex Shader）的视差映射开发细则：编写自定义的 THREE.ShaderMaterial 材质。算法实现：将上传好的原图作为 uTexture 采样器，深度图 PNG 作为 uDepthTexture 采样器传入着色器中。着色器逻辑：在顶点着色器（Vertex Shader）中，根据网格顶点的 UV 坐标去采样深度图的灰度值（采样值 $Z \in [0, 1]$），随后在反投影阶段，将顶点的实际位置沿 $Z$ 轴进行偏移：transformed.z += (depthValue * disparityScale);。以此在 WebGL 中使用一个简单的平面网格（PlaneGeometry）实时撑开生成 3D 浮雕空间。任务 5.4：WebGL 边缘扩展与空洞填充开发细则：由于视角拖拽旋转时边缘会出现由于拉伸导致的网格拉丝和“空洞”现象。前端优化：在着色器中，当检测到顶点的深度梯度大于设定的阈值时（代表物体的边缘断层处），在片元着色器（Fragment Shader）中采用边缘像素多重采样混合（Edge Extend）或微弱的模糊扩散进行后处理，以此保证实时渲染帧率稳定维持在 $\ge 30$ FPS 以上。步骤六：高级动画、图像增强与媒体导出（第 6 周）任务 6.1：基于 Tween.js 的相机动态轨迹动画引擎开发细则：在前端封装 AnimationEngine 动效驱动类。模板实现：使用 @tweenjs/tween.js 库，根据文档设计的预设模板来动态修改 Three.js 相机的 position 和 lookAt 目标点：摇摆模板 (TPL-001)：利用正弦函数 Math.sin(time) * amplitude 让相机在水平 $X$ 轴上来回轻微摆动。缩放模板 (TPL-002)：让相机在 $Z$ 轴上缓慢推进和拉远，实现富有呼吸感的 3D 变焦效果。任务 6.2：Real-ESRGAN 超分辨率与多队列集成开发细则：在后端 ai-models/enhance/ 下配置 Real-ESRGAN x4plus 模型。多任务编排：若用户在前台控制面板中勾选了“超分辨率”或“前景增强”选项，后端接收到 /api/v1/enhance 请求后，将超分任务投递至专有的 gpu_enhance 队列中（设置并发上限为 1，因超分极其消耗显存）。推理成功后，更新 images 表的 enhanced_url 字段。任务 6.3：媒体文件录制与导出服务（/api/v1/export）开发细则：当用户点击导出 MP4 或 GIF 时，系统提供两种实现路线：路线 A（前端方案，适合节省算力）：使用 ffmpeg.wasm，通过 Canvas 的 captureStream 录制前端正在播放动画的 WebGL 画面，并在浏览器沙箱内直接合并编码为 MP4 供用户下载。路线 B（后端方案，推荐，质量更高）：在后端启动一个专门跑 export 队列的 Celery Worker。该 Worker 利用 headless 无头浏览器（如 Puppeteer）静默打开当前的 3D 渲染页面，自动播放动画并通过 Node/Python 管道截取每一帧的 Canvas 画面，最终调用系统的 ffmpeg 命令行工具压制成标准的 1080p、30fps MP4 视频，存入 MinIO 的 exports/ 中。任务 6.4：MongoDB 分享链生成与嵌入代码开发细则：开发 /api/v1/share 接口。逻辑实现：用户生成公开分享时，将当前场景的 scene_id、动画参数以及过期时间等元数据写入 MongoDB shares 集合中，生成一个随机加密的 share_id 字符串（如 shr_vwx234）。前端开发一个不需要登录权限的独立路由页面 /share/:id，该页面直接读取对应的 JSON 并挂载精简版的 Three.js 画布，完美复刻无缝网页 3D 预览与 iframe 嵌入功能。开发建议与避坑指南交叉依赖地狱：在第三周拉取 AI 模型时，Depth Anything V2 与 Real-ESRGAN 依赖的 PyTorch 版本及 CUDA 运行时版本可能存在细微冲突。强烈建议不要把所有模型塞进同一个 FastAPI 环境，可以利用 Python 的虚拟环境隔离，或者干脆在部署时将 Celery 划分为多个独立的 Docker 镜像（即 worker-depth 和 worker-enhance 使用不同的基础镜像基础包）。显存溢出风险：由于大图超分辨率和高精度深度估计会瞬时榨干 GPU 显存，必须确保 CELERY_WORKER_CONCURRENCY 严格按照你文档中规定的上限来设置（通常显存为 24G 的卡，深度估计并发设为 2 已经是极限）。前置项依赖：请先集中火力保证 步骤一 到 步骤三 在本地跑通。只要能成功上传一张图片并在前端通过 WebSocket 收到后端处理完发回的灰度深度图，这个项目的技术骨架就已经成功了 70%，后续的 3D 网格微调和动画参数开发就可以完全交给前端独立迭代。