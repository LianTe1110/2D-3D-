# LeiaPix AI — 剩余开发任务清单

> 基于 `复刻路线.md` 和 `LeiaPix_AI_复刻完整版开发文档.md` 的完整对照分析。
> 步骤一至六的代码框架已全部完成，以下为剩余工作。

---

## 优先级一：端到端集成打通（核心闭环）

> 目标：让"上传 → 深度估计 → 3D渲染 → 导出 → 分享"全链路在本地完整跑通。

### P1-1. render/3d API 与前端 Scene3D 对接

**现状**：前端 Scene3D 直接用 `imageUrl + depthMapUrl` 渲染，但后端 `POST /api/v1/render/3d` 创建的 scene 记录未被前端消费。

**需要做的**：
- 深度估计完成后，前端自动调用 `POST /api/v1/render/3d`，传入 `image_id` + `depth_map_id`
- 后端返回 `scene_id` + `render_params`，前端存入 store
- Scene3D 组件从 scene 数据中读取 `texture_url` + `depth_map_url` + `render_params`

**涉及文件**：
- `frontend/src/stores/editor-store.ts` — 新增 `sceneId` 状态 + `initScene()` action
- `frontend/src/services/api.ts` — 新增 `initScene()` API 调用
- `frontend/src/components/preview/Scene3D.tsx` — 从 store 读取 scene 数据
- `frontend/src/types/index.ts` — 新增 `RenderRequest/RenderResponse` 类型

---

### P1-2. editor-store 集成 scene_id 流转

**现状**：store 只有 `uploadedImage` + `depthResult`，缺少 `sceneId` 状态。导出和分享都需要 scene_id。

**需要做的**：
- store 新增字段：`sceneId: string | null`
- 深度估计成功后自动调用 `initScene(imageId, depthMapId)` → 拿到 sceneId
- 导出面板和分享面板使用真实 sceneId 替代 alert 占位

**涉及文件**：
- `frontend/src/stores/editor-store.ts`
- `frontend/src/pages/EditorPage.tsx` — 导出/分享按钮绑定 sceneId

---

### P1-3. 前端导出面板绑定真实 scene_id

**现状**：`exportMedia()` 接收 sceneId 参数，但 EditorPage 中传入的是占位值。

**需要做的**：
- EditorPage 中从 store 读取 `sceneId`，传给 `exportMedia(sceneId, options)`
- 后端导出按钮使用真实 sceneId
- 前端 ffmpeg.wasm 导出也需要 sceneId（用于获取深度图）

---

### P1-4. 前端分享面板绑定真实 scene_id

**现状**：`createShareLink()` 接收 sceneId 参数，但 EditorPage 中是 `alert()` 占位。

**需要做的**：
- EditorPage 中从 store 读取 `sceneId`，传给 `createShareLink(sceneId, title)`
- 移除 alert 占位，改为真实调用

---

## 优先级二：开发文档已设计但未实现的功能

### P2-1. Nginx 反向代理配置

**来源**：开发文档第 11 章

**需要做的**：
- 编写 `deploy/docker/nginx.conf`
- 配置 SSL/TLS（开发环境可用 mkcert 生成本地证书）
- 静态资源服务（前端 build 产物）
- 反向代理后端 API + WebSocket
- 负载均衡（多 Backend 实例）
- Gzip 压缩
- 安全 Headers（X-Frame-Options, CSP 等）
- 更新 `docker-compose.yml` 新增 nginx 服务

---

### P2-2. CI/CD 流水线

**来源**：开发文档第 12 章

**需要做的**：
- 编写 `.github/workflows/ci.yml`
  - frontend-ci: checkout → npm ci → lint → typecheck → test → build
  - backend-ci: checkout → pip install → ruff check → mypy → pytest
  - docker-build: 构建镜像 + 推送镜像仓库
- 编写 `.github/workflows/cd.yml`
  - 触发条件: push 到 main
  - 步骤: 构建镜像 → 推送 → kubectl 滚动更新 → rollout status

---

### P2-3. K8s 部署 YAML

**来源**：开发文档第 11 章

**需要做的**：
- `deploy/k8s/namespace.yaml` — leiapix 命名空间
- `deploy/k8s/frontend-deployment.yaml` — 前端 Pod (2 replicas)
- `deploy/k8s/backend-deployment.yaml` — 后端 Pod (3 replicas)
- `deploy/k8s/worker-deployment.yaml` — CPU Worker Pod (3 replicas)
- `deploy/k8s/gpu-deployment.yaml` — GPU Worker Pod (4 replicas)
- `deploy/k8s/ingress.yaml` — Ingress 路由规则
- `deploy/k8s/configmap.yaml` — 环境变量 ConfigMap

---

### P2-4. Prometheus + Grafana 监控

**来源**：开发文档第 13 章

**需要做的**：
- 后端集成 `prometheus-fastapi-instrumentator` 暴露 `/metrics`
- 编写 `deploy/docker/prometheus.yml` 采集配置
- 编写 Grafana Dashboard JSON（API 指标 + GPU 指标 + 业务指标）
- docker-compose 新增 prometheus + grafana 服务
- 告警规则（CPU>80%、GPU显存>90%、API P95>500ms、队列积压>100）

**监控指标清单**：

| 类别 | 指标 | 告警阈值 |
|------|------|----------|
| 系统 | CPU 使用率 | > 80% 持续 5 分钟 |
| 系统 | 内存使用率 | > 85% |
| 系统 | GPU 显存使用率 | > 90% |
| 系统 | 磁盘使用率 | > 85% |
| 应用 | API 响应时间 P95 | > 500ms |
| 应用 | API 错误率 | > 1% |
| 应用 | 任务队列积压 | > 100 待处理 |
| 业务 | 深度估计成功率 | < 95% |
| 业务 | 导出成功率 | < 98% |

---

### P2-5. 测试用例编写

**来源**：开发文档第 10 章

**需要做的**：

**后端测试** (`backend/tests/`)：
- `test_upload.py` — 上传校验、EXIF旋转、缩略图生成
- `test_depth.py` — 深度估计API + 任务状态查询
- `test_render.py` — 3D场景初始化
- `test_enhance.py` — 图像增强API
- `test_export.py` — 导出API
- `test_share.py` — 分享CRUD
- `test_auth.py` — 注册/登录/刷新/鉴权

**前端测试** (`frontend/src/__tests__/`)：
- 组件单元测试 (Jest + Testing Library)
- Store 状态机测试

**E2E 测试** (`e2e/`)：
- Playwright 全流程: 上传 → 深度 → 3D → 导出 → 分享

**AI 模型测试**：
- 深度精度、边缘质量、推理速度、显存占用、鲁棒性

---

### P2-6. 套餐配额限流

**来源**：开发文档第 8 章

**需要做的**：
- `backend/app/api/deps.py` 中 `RateLimiter` 按 user.plan 区分配额
- 配额表：

| 端点 | 免费用户 | Pro 用户 | 企业用户 |
|------|----------|----------|----------|
| /upload | 10 次/小时 | 50 次/小时 | 无限 |
| /depth/estimate | 5 次/小时 | 30 次/小时 | 无限 |
| /enhance | 3 次/小时 | 20 次/小时 | 无限 |
| /export | 5 次/小时 | 30 次/小时 | 无限 |
| /share | 10 次/小时 | 50 次/小时 | 无限 |

- Redis 存储配额计数器（key: `rate:{user_id}:{endpoint}`, TTL: 1h）
- 4K 导出限制：免费用户提示升级 (QUOTA_EXCEEDED 429)

---

## 优先级三：增强功能

### P3-1. SAM 图像分割

**现状**：`ai-models/segmentation/sam/` 只有 `.gitkeep`

**需要做的**：
- 集成 Segment Anything Model (SAM)
- 编写 `ai-models/segmentation/sam/engine.py`
- 在 `model_manager.py` 注册 SAM 模型
- 后端新增 `/api/v1/segment` API
- 前端新增分割交互（点击选区）

---

### P3-2. MiDaS / LeRes 深度模型

**现状**：`ai-models/depth/midas/` + `leres/` 只有 `.gitkeep`

**需要做的**：
- 编写 MiDaS 和 LeRes 推理引擎
- 在 model_manager 注册
- 前端深度估计时可选择模型

---

### P3-3. 前端性能优化

**来源**：开发文档第 5.4 节 + 第 9.2 节

**需要做的**：
- 路由懒加载 + 代码分割 (React.lazy + Suspense)
- 纹理渐进式加载 + mipmap
- Three.js 资源及时 dispose()
- 移动端 devicePixelRatio 适配
- 深度图压缩 + WebWorker 解压

---

### P3-4. AI 推理优化

**来源**：开发文档第 9.2 节

**需要做的**：
- 模型 INT8 量化
- TensorRT 加速
- 批处理推理
- 模型缓存（避免重复加载）

---

## 优先级四：运维与安全

### P4-1. 数据库备份策略

| 数据 | 备份频率 | 保留时长 |
|------|----------|----------|
| PostgreSQL | 每日全量 + 实时 WAL | 30 天 |
| MongoDB | 每日全量 | 30 天 |
| MinIO 文件 | 每日增量 | 90 天 |
| Redis | 不备份（可重建） | - |

---

### P4-2. 安全加固

- HTTPS 强制跳转
- CSP (Content-Security-Policy) Headers
- 文件上传二次校验（Magic Bytes + PIL 检测）
- SQL 注入防护（SQLAlchemy 参数化查询 ✓）
- XSS 防护（React 自动转义 ✓）
- CSRF Token
- API 滥用防护（RateLimiter ✓，需按套餐细化）

---

## 待下载的模型权重

| 模型 | 权重文件 | 目标目录 | 大小 | 下载方式 |
|------|----------|----------|------|----------|
| Depth Anything V2 | `depth_anything_v2_vitl.pth` | `ai-models/depth/depth_anything_v2/weights/` | 1.28GB | ✅ 已下载 |
| Real-ESRGAN | `RealESRGAN_x4plus.pth` | `ai-models/enhance/real_esrgan/weights/` | ~64MB | ❌ 待下载 |

**Real-ESRGAN 下载命令**：
```bash
# 从 HuggingFace 镜像下载
cd ai-models/enhance/real_esrgan/weights/
curl -L -o RealESRGAN_x4plus.pth https://hf-mirror.com/ai-forever/Real-ESRGAN/resolve/main/RealESRGAN_x4plus.pth
```

---

## 待安装的 Python 依赖

```bash
# MongoDB 异步驱动 + ODM
pip install motor beanie

# Real-ESRGAN 依赖
pip install basicsr realesrgan facexlib gfpgan

# 导出 Worker 依赖
pip install playwright
playwright install chromium

# 监控
pip install prometheus-fastapi-instrumentator

# 测试
pip install pytest pytest-asyncio httpx
```

---

## 项目进度总览

| 步骤 | 阶段 | 状态 |
|------|------|------|
| 步骤一 | 基础设施与脚手架 | ✅ 完成 |
| 步骤二 | 数据模型与迁移 | ✅ 完成 |
| 步骤三 | AI 推理与异步任务 | ✅ 完成 |
| 步骤四 | 后端 RESTful API | ✅ 完成 |
| 步骤五 | 前端 3D 渲染引擎 | ✅ 完成 |
| 步骤六 | 动画/增强/导出/分享 | ✅ 完成 |
| — | 端到端集成打通 | ❌ P1 待做 |
| — | Nginx 反向代理 | ❌ P2 待做 |
| — | CI/CD 流水线 | ❌ P2 待做 |
| — | K8s 部署 | ❌ P2 待做 |
| — | Prometheus + Grafana | ❌ P2 待做 |
| — | 测试用例 | ❌ P2 待做 |
| — | 套餐配额限流 | ❌ P2 待做 |
| — | SAM 分割 | ❌ P3 待做 |
| — | MiDaS/LeRes 模型 | ❌ P3 待做 |
| — | 前端性能优化 | ❌ P3 待做 |
| — | AI 推理优化 | ❌ P3 待做 |
| — | 运维与安全 | ❌ P4 待做 |
